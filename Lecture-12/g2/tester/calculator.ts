
const next = (a: number) => a + 1;

export const add = (a: number, b: number) => {
    // let result = a;
    // for (let index = 0; index < b; index = next(index)) {
    //     result = next(result);
    // }
    // return result;

    return a + b;
}