export const next = (n: number) => n + 1;

export const add = (a: number, b: number) => {
    /// the implementation goes here

    // let result = a;
    // for (let i = 0; i < b; i = next(i)) {
    //     result = next(result);
    // }

    // return result;

    return a + b;
}