const weko = {
    name: 'Wekoslav',
    surname: 'Stefanovski',
    age: 46,
    // toString() {
    //     return `${this.name} ${this.surname} (${this.age})`;
    // }
};

console.log(weko.toString());

console.log(weko.name);

export {};