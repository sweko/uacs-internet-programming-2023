const weko = {
    name: 'Wekoslav',
    age: 46,
    occupation: 'lecturer',
    // toString: function() {
    //     return `${this.name} (${this.age}) is a ${this.occupation}`;
    // }
};

console.log(weko.toString());

// Prototype === Object.prototype

export {};